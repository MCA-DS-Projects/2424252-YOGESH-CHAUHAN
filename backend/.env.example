# EduNexa LMS Backend Environment Variables

# Database Configuration
MONGO_URI=mongodb://localhost:27017/edunexa_lms

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production

# Email/Notification Configuration
# The system uses Gmail SMTP to send email notifications for:
# - Assignment creation, updates, and deletion
# - Assignment submissions and grading
# - Course enrollment and other system events
# - Role-specific notifications (student, teacher, admin)
#
# To use Gmail, you MUST create an App Password (not your regular Gmail password):
# 1. Go to https://myaccount.google.com/
# 2. Navigate to Security > 2-Step Verification (enable if not already)
# 3. Go to Security > App passwords
# 4. Select "Mail" and "Other (Custom name)"
# 5. Generate the 16-character app password
# 6. Copy the password (remove spaces) and paste below
#
# For detailed setup instructions, see: backend/NOTIFICATION_SYSTEM_README.md
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_ADDRESS=your-email@gmail.com
EMAIL_PASSWORD=your-16-char-app-password-no-spaces
EMAIL_USE_TLS=true

# Notification Feature Toggles
# Enable/disable email and in-app notifications globally
ENABLE_EMAIL_NOTIFICATIONS=true
ENABLE_IN_APP_NOTIFICATIONS=true
NOTIFICATION_FROM_NAME=EduNexa LMS

# AI Configuration (Optional - for enhanced AI features)
GEMINI_API_KEY=your-google-gemini-api-key-here

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True
PORT=5000

# Note: If GEMINI_API_KEY is not provided, the system will use fallback responses
# To get a Gemini API key, visit: https://makersuite.google.com/app/apikey